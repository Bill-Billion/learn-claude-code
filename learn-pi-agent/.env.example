# Required: your OpenAI API key (or a key accepted by a compatible endpoint).
OPENAI_API_KEY=

# Optional. Defaults to gpt-4o-mini.
# OPENAI_MODEL=gpt-4o-mini

# Optional. Defaults to the official OpenAI API.
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional. Use "always" only for project-controlled .pi resources; defaults to "ask".
# PI_PROJECT_TRUST=ask

# 本仓库安装了 langchain-openai，开箱支持 OpenAI。
# 使用其他 provider 时，需要另行安装对应的 LangChain 集成包并配置密钥。
LANGCHAIN_MODEL=openai:gpt-4.1-mini

# s11-s13 默认还会使用 OpenAIEmbeddings；除非显式注入其他 embeddings 实现，
# 否则这些课程同样需要 OPENAI_API_KEY。
OPENAI_API_KEY=
